version: '3.9'

services:
  postgresql:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clubapp
    volumes:
      - pgdata:/var/lib/postgresql/data    # volume Docker géré

volumes:
  pgdata:
